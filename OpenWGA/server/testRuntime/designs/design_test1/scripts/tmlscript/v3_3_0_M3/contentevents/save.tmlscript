content().setItemValue("saveContentEvent", "done");
if (content() != null && !content().isDummy() && content() == event.document) {
	content().setItemValue("saveContentDocumentAccessible", "true");	
}